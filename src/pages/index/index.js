'use strict';

import React from 'react';
import { layout, typography } from '~/theme';

const Home = (props) => {
	return (
		<layout.Wrap>
			<typography.H1>Spotify Widget</typography.H1>
		</layout.Wrap>
	);
};

export default Home;
